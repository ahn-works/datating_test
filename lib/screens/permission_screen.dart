import 'package:flutter/material.dart';
import 'profile_setup_screen.dart';
class PermissionScreen extends StatelessWidget { const PermissionScreen({super.key}); final _perms = const [[Icons.location_on, '위치 정보', '동네 여행 모임 탐색에 필요합니다', '필수'], [Icons.camera_alt, '카메라', '미션 인증샷 업로드에 필요합니다', '필수'], [Icons.notifications, '알림', '매칭 알림 및 다음 단계 안내를 받습니다', '선택']]; @override Widget build(BuildContext context) => Scaffold(backgroundColor: Colors.white, body: SafeArea(child: Padding(padding: const EdgeInsets.all(32), child: Column(children: [const Spacer(), const Text('아래 권한이\n필요합니다', style: TextStyle(fontSize: 24, fontWeight: FontWeight.bold, height: 1.4), textAlign: TextAlign.center), const SizedBox(height: 40), ..._perms.map((p) => Padding(padding: const EdgeInsets.only(bottom: 20), child: Row(children: [Container(padding: const EdgeInsets.all(12), decoration: BoxDecoration(color: const Color(0xFFFFF5F5), shape: BoxShape.circle), child: Icon(p[0] as IconData, color: const Color(0xFFFF3B30))), const SizedBox(width: 16), Expanded(child: Column(crossAxisAlignment: CrossAxisAlignment.start, children: [Row(children: [Text(p[1] as String, style: const TextStyle(fontWeight: FontWeight.bold)), const SizedBox(width: 8), Container(padding: const EdgeInsets.symmetric(horizontal: 6, vertical: 2), decoration: BoxDecoration(color: const Color(0xFFFF3B30), borderRadius: BorderRadius.circular(4)), child: Text(p[3] as String, style: const TextStyle(color: Colors.white, fontSize: 10)))]), Text(p[2] as String, style: const TextStyle(color: Colors.grey, fontSize: 13))]))]))), const Spacer(), ElevatedButton(onPressed: () => Navigator.push(context, MaterialPageRoute(builder: (_) => const ProfileSetupScreen())), style: ElevatedButton.styleFrom(backgroundColor: Colors.black, minimumSize: const Size(double.infinity, 56), shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(16))), child: const Text('가입하기', style: TextStyle(color: Colors.white, fontWeight: FontWeight.bold, fontSize: 16))), ])))); }
